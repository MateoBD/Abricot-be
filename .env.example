# --- Flask config ---
FLASK_DEBUG=True
FLASK_APP=application.py

# --- Logger config ---
LOGGER_LEVEL=DEBUG
LOGGER_VERBOSE=TRUE

# --- JWT config ---
# Secret key used to sign JWT tokens. Use a long random string in production.
JWT_SECRET_KEY=change-me-in-production

# --- PostgreSQL config ---
# for more details see https://hub.docker.com/_/postgres#environment-variables
# If any configuration is changed make sure to stop and restart the PostgreSQL container
POSTGRES_DB=dbname
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=admin
POSTGRES_PASSWORD=example
