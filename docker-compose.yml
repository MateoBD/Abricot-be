services:
  db:
    image: postgres:16
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    env_file:
      - ".env"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
